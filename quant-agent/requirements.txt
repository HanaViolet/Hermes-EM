# Unified requirements for the full project (trading_agent + trading_server + ClawLibrary backend)
# Install: pip install -r requirements.txt

# ── Web framework ──
flask>=3.0
flask-cors>=4.0

# ── Data & computation ──
pandas>=2.0
numpy>=1.24
scipy>=1.11
requests>=2.31
python-dotenv>=1.0

# ── Visualization & reports ──
plotly>=5.18
pyyaml>=6.0

# ── Streamlit standalone UI ──
streamlit>=1.28

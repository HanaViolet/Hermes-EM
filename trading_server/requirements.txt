# NOTE: Prefer the root requirements.txt which includes all deps for both agent and server.
# pip install -r ../requirements.txt
flask>=3.0
flask-cors>=4.0
pandas>=2.0
numpy>=1.24
plotly>=5.18
requests>=2.31
